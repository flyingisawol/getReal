import { useEffect } from "react"
import { Link } from "react-router-dom"

const ProfileLink = ({ user }) => {
  //we want to have a param/variable in the url link that sends the logged in user to their own showpage/profile

  
 
  // if (user. === profile.creator.username) {
  //   return <Link to="/api/getreal/:">Profile</Link>
  // }
}

export default ProfileLink